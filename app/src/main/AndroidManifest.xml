<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.frotch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activity.DuvidaActivity"
            android:label="@string/txt_btn_duvidas" />
        <activity
            android:name=".activity.InfoActivity"
            android:label="@string/txt_btn_info" />
        <activity
            android:name=".activity.VeiculosActivity"
            android:label="@string/txt_btn_listarVeiculos" />
        <activity
            android:name=".activity.CalendarioActivity"
            android:label="@string/txt_btn_calendario" />

        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme.Fullscreen"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity
            android:name=".activity.SplashscreenActivity"
            android:theme="@style/AppTheme.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        </activity>
    </application>
</manifest>